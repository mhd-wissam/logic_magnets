from board import Board
from cell import Cell
from stone import Stone
from stage import Stage
from bfs import Bfs
from dfs import Dfs
from tree import Tree
from player import Player
import random
import string
if __name__ == "__main__":

    Player.my_self()






















    # stage=Stage.board28
    # stage.display()
    # node=Tree(stage)
    # visted=[]
    # # visted.append(stage)
    # x=node.creat_tree()

    # y=Dfs.dfs(x)
    # if y != None :
    #     y.value.display()
    # else :print("you loser")        
    # y.value.display()
    # stack=[]
    # stack.append(y)

    # while(stack[-1].parent !=None):
    #     stack.append(stack[-1].parent)

    # while stack:
    #     # Peek at the top element
    #     top_element = stack[-1]
    #     print("Top element:")  # Output will show the top element each iteration
        
    #     # Pop the top element after processing
    #     stack.pop().value.display()


    # x.children[0].value.display()
    # print("\n...........................................\n")

    # x1=x.children[0].creat_tree()
    # node.chiled_of_child()
    # x1=x.children[0]


    # def print_tree(node):
    #     father = node.value
    #     print("Father:")
    #     father.display()
    #     print("...........................................\n")
    #     # Use a counter to number the children as "child1:", "child2:", etc.
    #     # for idx, child in enumerate(node.children, start=1):
    #     #     print(f"Child{idx}:")
    #     #     child.value.display()
    #     #     print("...................................................\n")

    # print_tree(x.children[7].children[26].children[0])     
    